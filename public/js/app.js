// CRM SPA - Phase 1 placeholder
document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  app.innerHTML = '<div class="loading">CRM Loading... Frontend will be built in Phase 4.</div>';
});
